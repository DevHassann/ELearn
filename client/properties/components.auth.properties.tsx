// LOGIN COMPONENT PROPERTIES
export type LoginComponentProperties = {
  setRoute: (route: string) => void;
};

// SIGN-UP COMPONENT PROPERTIES
export type SignUpComponentProperties = {
  setRoute: (route: string) => void;
};

// VERIFICATION COMPONENT PROPERTIES
export type VerificationComponentProperties = {
  setRoute: (route: string) => void;
};

// VERIFICATION COMPONENT > VERIFY NUMBERS PROPERTIES
export type VerifyNumbersProperties = {
  "0": string;
  "1": string;
  "2": string;
  "3": string;
};
