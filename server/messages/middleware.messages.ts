const AuthenticationMessage = "Please login to access this resource";

const InvalidAccessTokenMessage = "Access token is not valid";

const NotFoundUserMessage = "User not found";

export {
  AuthenticationMessage,
  InvalidAccessTokenMessage,
  NotFoundUserMessage,
};
